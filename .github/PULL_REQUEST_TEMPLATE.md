<!-- What this changes and why. Link any related issue. -->

## What & why

## Testing
- [ ] `tests/run.sh` passes
- [ ] `cd agent && bun test` passes
- [ ] `cd pager && swift build` passes (if Pager was touched)
- [ ] `shellcheck --severity=warning <files>` clean (if a shell script was touched)

<!-- Reminder: a new file under agent/ must also be added to agent/MANIFEST.txt
     and install.sh's copy list in the SAME PR (CI's manifest test enforces this). -->
